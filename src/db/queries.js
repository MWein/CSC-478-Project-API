export const selectAllRows = () => ({
  text: 'SELECT * FROM "test_table"',
})

// module.exports.selectEvent = id => ({
//   text: 'SELECT * FROM "table_name" WHERE event_id = ($1)',
//   values: [ id ],
// })
